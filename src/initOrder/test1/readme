Дан следующий код.
public class Test { 
    class A { 
        String str = "ab"; 
 
        A() { 
            printLength(); 
        } 
 
        void printLength() { 
            System.out.println(str.length()); 
        } 
    } 
 
    class B extends A { 
        String str = "abc"; 
 
        void printLength() { 
            System.out.println(str.length()); 
        } 
    } 
 
    public static void main(String[] args) { 
        new Test().new B(); 
    } 
}

Как вы думаете, что произойдет в результате выполнения данного кода?
NullPointerException - ПОЯСНЕНИЯ В КОДЕ - см. проставленный порядок выполнения кода